<x-layouts.app.sidebar>
    <div class="max-w-3xl space-y-6"><div><a href="{{ route('groups.show', $group->id) }}" class="text-sm text-sky-600 hover:underline">← Kembali ke grup</a><h1 class="mt-2 text-2xl font-semibold">Anggota {{ $group->name }}</h1></div>@include('partials.flash')<section class="overflow-hidden rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900"><div class="divide-y divide-zinc-200 dark:divide-zinc-700">@foreach ($group->users as $member)<div class="flex items-center justify-between gap-4 px-5 py-4"><div><p class="font-medium">{{ $member->name }}</p><p class="text-sm text-zinc-500">{{ $member->email }}</p></div><div class="flex items-center gap-3"><span class="rounded-full bg-zinc-100 px-2 py-1 text-xs capitalize dark:bg-zinc-800">{{ $member->pivot->role }}</span>@if ($member->id !== $group->owner_id && $member->id !== auth()->id())<form method="POST" action="{{ route('groups.members.kick', [$group->id, $member->id]) }}" onsubmit="return confirm('Keluarkan anggota ini?')">@csrf @method('DELETE')<button class="text-sm text-rose-600 hover:underline">Keluarkan</button></form>@endif</div></div>@endforeach</div></section></div>
</x-layouts.app.sidebar>