<x-layouts.app.sidebar>
    <div class="space-y-6"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-zinc-500">Kolaborasi</p><h1 class="text-2xl font-semibold">Grup</h1></div><div class="flex gap-2"><a href="{{ route('groups.create') }}" class="rounded-lg bg-zinc-900 px-4 py-2 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">+ Buat grup</a><details class="relative"><summary class="cursor-pointer list-none rounded-lg border border-zinc-300 px-4 py-2 text-sm font-medium dark:border-zinc-600">Gabung</summary><form method="POST" action="{{ route('groups.join') }}" class="absolute right-0 z-10 mt-2 flex w-72 gap-2 rounded-xl border border-zinc-200 bg-white p-4 shadow-lg dark:border-zinc-700 dark:bg-zinc-900">@csrf<input required name="invite_code" placeholder="Kode undangan" class="min-w-0 flex-1 rounded-lg border-zinc-300 uppercase dark:border-zinc-600 dark:bg-zinc-800"><button class="rounded-lg bg-sky-600 px-3 py-2 text-sm text-white">Gabung</button></form></details></div></div>@include('partials.flash')<div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse ($groups as $group)<a href="{{ route('groups.show', $group->id) }}" class="rounded-xl border border-zinc-200 bg-white p-5 hover:border-sky-400 dark:border-zinc-700 dark:bg-zinc-900"><div class="flex items-start justify-between gap-3"><h2 class="font-semibold">{{ $group->name }}</h2><span class="text-xs text-zinc-500">{{ $group->users->count() }} anggota</span></div><p class="mt-5 text-2xl font-semibold">Rp {{ number_format($group->wallet?->balance ?? 0, 0, ',', '.') }}</p><p class="mt-1 text-xs text-zinc-500">Saldo grup</p></a>@empty<div class="rounded-xl border border-dashed border-zinc-300 px-6 py-12 text-center text-sm text-zinc-500 md:col-span-2 xl:col-span-3">Kamu belum tergabung dalam grup.</div>@endforelse</div><div>{{ $groups->links() }}</div></div>
</x-layouts.app.sidebar>