<x-layouts.app.sidebar>
    <div class="max-w-3xl space-y-6"><div><a href="{{ route('wallets.show', $wallet->id) }}" class="text-sm text-sky-600 hover:underline">← Kembali ke dompet</a><h1 class="mt-2 text-2xl font-semibold">Edit {{ $wallet->name }}</h1></div>@include('partials.flash')<form method="POST" action="{{ route('wallets.update', $wallet->id) }}" class="grid gap-4 rounded-xl border border-zinc-200 bg-white p-6 dark:border-zinc-700 dark:bg-zinc-900">@csrf @method('PUT')<label class="grid gap-1 text-sm">Nama dompet<input required name="name" value="{{ old('name', $wallet->name) }}" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label><label class="grid gap-1 text-sm">Anggaran total<input type="number" min="0" step="0.01" name="budget_limit" value="{{ old('budget_limit', $wallet->budget_limit) }}" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label><fieldset class="space-y-3"><legend class="font-medium">Anggaran kategori</legend>@forelse ($categories as $category)<label class="grid gap-1 text-sm">{{ $category->categories }}<input type="number" min="0" step="0.01" name="categories[{{ $category->id }}][limit_amount]" value="{{ old('categories.'.$category->id.'.limit_amount', $category->limit_amount ?? 0) }}" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label>@empty<p class="text-sm text-zinc-500">Belum ada kategori untuk dompet ini.</p>@endforelse</fieldset><div class="flex flex-wrap gap-2"><button class="rounded-lg bg-zinc-900 px-4 py-2 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">Simpan</button></div></form><form method="POST" action="{{ route('wallets.destroy', $wallet->id) }}" onsubmit="return confirm('Hapus dompet ini?')">@csrf @method('DELETE')<button class="text-sm text-rose-600 hover:underline">Hapus dompet</button></form></div>
</x-layouts.app.sidebar>