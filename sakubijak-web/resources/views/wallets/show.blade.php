<x-layouts.app.sidebar>
    <div class="space-y-6"><div><a href="{{ route('wallets.index') }}" class="text-sm text-sky-600 hover:underline">← Semua dompet</a><div class="mt-2 flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-zinc-500">{{ $wallet->type === 'group' ? $wallet->group?->name : 'Dompet personal' }}</p><h1 class="text-2xl font-semibold">{{ $wallet->name }}</h1></div><a href="{{ route('wallets.edit', $wallet->id) }}" class="rounded-lg bg-zinc-900 px-4 py-2 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">Edit dompet</a></div></div>@include('partials.flash')<div class="grid gap-4 md:grid-cols-3"><div class="rounded-xl bg-zinc-900 p-5 text-white dark:bg-zinc-950"><p class="text-sm text-zinc-400">Saldo saat ini</p><p class="mt-2 text-2xl font-semibold">Rp {{ number_format($wallet->balance, 0, ',', '.') }}</p></div><div class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-700 dark:bg-zinc-900"><p class="text-sm text-zinc-500">Anggaran total</p><p class="mt-2 text-2xl font-semibold">Rp {{ number_format($wallet->budget_limit ?? 0, 0, ',', '.') }}</p></div><div class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-700 dark:bg-zinc-900"><p class="text-sm text-zinc-500">Transaksi</p><p class="mt-2 text-2xl font-semibold">{{ $transactions->total() }}</p></div></div><section class="rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900"><h2 class="border-b border-zinc-200 px-5 py-4 font-semibold dark:border-zinc-700">Riwayat transaksi</h2><div class="divide-y divide-zinc-200 dark:divide-zinc-700">@forelse ($transactions as $transaction)<a href="{{ route('transactions.show', $transaction->id) }}" class="flex items-center justify-between gap-4 px-5 py-4 hover:bg-zinc-50 dark:hover:bg-zinc-800"><div><p class="font-medium">{{ $transaction->description ?: 'Transaksi tanpa keterangan' }}</p><p class="text-xs text-zinc-500">{{ $transaction->date?->format('d M Y H:i') }} · {{ $transaction->categories?->categories ?: $transaction->category?->categories }}</p></div><span class="font-semibold {{ $transaction->type === 'income' ? 'text-emerald-600' : 'text-rose-600' }}">{{ $transaction->type === 'income' ? '+' : '-' }} Rp {{ number_format($transaction->amount, 0, ',', '.') }}</span></a>@empty<p class="px-5 py-10 text-center text-sm text-zinc-500">Belum ada transaksi.</p>@endforelse</div><div class="p-5">{{ $transactions->links() }}</div></section></div>
</x-layouts.app.sidebar>