<x-layouts.app.sidebar>
    <div class="max-w-2xl space-y-6"><div><a href="{{ route('transactions.index') }}" class="text-sm text-sky-600 hover:underline">← Semua transaksi</a><h1 class="mt-2 text-2xl font-semibold">Detail transaksi</h1></div><section class="rounded-xl border border-zinc-200 bg-white p-6 dark:border-zinc-700 dark:bg-zinc-900"><div class="flex items-start justify-between gap-4"><div><p class="text-sm text-zinc-500">{{ $transaction->date?->format('d M Y H:i') }}</p><h2 class="mt-1 text-lg font-semibold">{{ $transaction->description ?: 'Transaksi tanpa keterangan' }}</h2></div><p class="text-xl font-semibold {{ $transaction->type === 'income' ? 'text-emerald-600' : 'text-rose-600' }}">{{ $transaction->type === 'income' ? '+' : '-' }} Rp {{ number_format($transaction->amount, 0, ',', '.') }}</p></div><dl class="mt-8 grid gap-4 border-t border-zinc-200 pt-5 text-sm dark:border-zinc-700"><div class="flex justify-between gap-4"><dt class="text-zinc-500">Dompet</dt><dd>{{ $transaction->wallet?->name }}</dd></div><div class="flex justify-between gap-4"><dt class="text-zinc-500">Kategori</dt><dd>{{ $transaction->category?->categories ?: $transaction->categories?->categories }}</dd></div><div class="flex justify-between gap-4"><dt class="text-zinc-500">Dibuat oleh</dt><dd>{{ $transaction->user?->name }}</dd></div><div class="flex justify-between gap-4"><dt class="text-zinc-500">Grup</dt><dd>{{ $transaction->group?->name ?: 'Personal' }}</dd></div>@if ($transaction->recipient_name)<div class="flex justify-between gap-4"><dt class="text-zinc-500">Penerima</dt><dd>{{ $transaction->recipient_name }}</dd></div>@endif</dl></section></div>
</x-layouts.app.sidebar>