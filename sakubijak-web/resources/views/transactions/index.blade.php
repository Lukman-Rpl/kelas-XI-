<x-layouts.app.sidebar>
    <div class="space-y-6"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-zinc-500">Aktivitas keuangan</p><h1 class="text-2xl font-semibold">Transaksi</h1></div><details class="relative"><summary class="cursor-pointer list-none rounded-lg bg-zinc-900 px-4 py-2 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">+ Transaksi</summary><form method="POST" action="{{ route('transactions.store') }}" class="absolute right-0 z-10 mt-2 grid w-80 gap-3 rounded-xl border border-zinc-200 bg-white p-4 shadow-lg dark:border-zinc-700 dark:bg-zinc-900">@csrf<label class="grid gap-1 text-sm">Dompet<select required name="wallet_id" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800">@foreach ($wallets as $wallet)<option value="{{ $wallet->id }}">{{ $wallet->name }}</option>@endforeach</select></label><label class="grid gap-1 text-sm">Kategori<select required name="category_id" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800">@foreach ($categories as $category)<option value="{{ $category->id }}">{{ $category->categories }}</option>@endforeach</select></label><div class="grid grid-cols-2 gap-2"><label class="grid gap-1 text-sm">Tipe<select name="type" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"><option value="expense">Pengeluaran</option><option value="income">Pemasukan</option></select></label><label class="grid gap-1 text-sm">Jumlah<input required type="number" min="0.01" step="0.01" name="amount" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label></div><label class="grid gap-1 text-sm">Tanggal<input required type="datetime-local" name="date" value="{{ now()->format('Y-m-d\\TH:i') }}" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label><label class="grid gap-1 text-sm">Keterangan<input name="description" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label><button class="rounded-lg bg-sky-600 px-3 py-2 text-sm font-medium text-white">Simpan transaksi</button></form></details></div>@include('partials.flash')<form method="GET" class="grid gap-3 rounded-xl border border-zinc-200 bg-white p-4 md:grid-cols-5 dark:border-zinc-700 dark:bg-zinc-900"><input name="search" value="{{ request('search') }}" placeholder="Cari transaksi..." class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800 md:col-span-2"><input type="date" name="start_date" value="{{ request('start_date') }}" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"><input type="date" name="end_date" value="{{ request('end_date') }}" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"><select name="type" class="rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"><option value="">Semua tipe</option><option value="income" @selected(request('type') === 'income')>Pemasukan</option><option value="expense" @selected(request('type') === 'expense')>Pengeluaran</option></select><button class="rounded-lg bg-zinc-100 px-3 py-2 text-sm font-medium dark:bg-zinc-800">Filter</button></form><section class="overflow-hidden rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900"><div class="divide-y divide-zinc-200 dark:divide-zinc-700">@forelse ($transactions as $transaction)<a href="{{ route('transactions.show', $transaction->id) }}" class="flex flex-wrap items-center justify-between gap-3 px-5 py-4 hover:bg-zinc-50 dark:hover:bg-zinc-800"><div><p class="font-medium">{{ $transaction->description ?: 'Transaksi tanpa keterangan' }}</p><p class="text-xs text-zinc-500">{{ $transaction->date?->format('d M Y H:i') }} · {{ $transaction->wallet?->name }} · {{ $transaction->category?->categories }}</p></div><span class="font-semibold {{ $transaction->type === 'income' ? 'text-emerald-600' : 'text-rose-600' }}">{{ $transaction->type === 'income' ? '+' : '-' }} Rp {{ number_format($transaction->amount, 0, ',', '.') }}</span></a>@empty<p class="px-5 py-10 text-center text-sm text-zinc-500">Belum ada transaksi yang cocok.</p>@endforelse</div><div class="p-5">{{ $transactions->links() }}</div></section></div>
</x-layouts.app.sidebar>